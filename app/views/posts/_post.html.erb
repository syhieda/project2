<div id="<%= dom_id post %>">
  <h2>
    <%= post.title %>
  </h2>

  <p>
    <%= post.content %>
  </p>

  <p>
    <%= image_tag(post.image.url, alt: 'Image', class: 'img-fluid') if post.image? %>
  </p>

    <p>
      <%= post.user.email if post.user %>
    </p>

    <p>
      <%= post.created_at %>
    </p>
</div>
